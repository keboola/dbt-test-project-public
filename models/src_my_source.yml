version: 2
sources:
    -
        name: my_source
        database: '{{ env_var("DBT_KBC_PROD_DATABASE") }}'
        schema: in.c-test-bucket
        tables:
            -
                name: test
                quoting:
                    database: true
                    schema: true
                    identifier: true
